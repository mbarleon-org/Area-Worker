# Area Worker — Documentation

This `docs/` directory contains developer and deployment documentation
for the Area Runner service.

Contents
- `BUILD.md` — how to build and run locally
- `DEPLOYMENT.md` — docker-compose and Kubernetes (KinD) deployment notes

If you're getting started, start with `docs/BUILD.md` to run the
service locally and run type checks.
