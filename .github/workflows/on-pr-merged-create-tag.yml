name: Create tag when PR into main is merged
on:
  pull_request:
    branches: [main]
    types: [closed]

permissions:
  contents: write
  packages: write
  id-token: write

jobs:
  tag-on-merge:
    if: ${{ github.event.pull_request.merged == true &&
            github.event.pull_request.base.ref == 'main'
        }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout main
        uses: actions/checkout@v6
        with:
          repository: ${{ github.repository }}
          ref: main
          fetch-depth: 0

      - name: Configure git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Create and push next tag (patch)
        id: create_tag
        run: |
          set -euo pipefail
          git fetch --tags origin

          LATEST=$(git tag --list 'v[0-9]*.[0-9]*.[0-9]*' --sort=-v:refname | head -n1)
          [ -z "$LATEST" ] && LATEST="v0.0.0"

          ver="${LATEST#v}"
          IFS='.' read -r MAJ MIN PAT <<<"$ver"
          NEXT="v${MAJ}.${MIN}.$((PAT+1))"

          while git rev-parse -q --verify "refs/tags/$NEXT" >/dev/null; do
            ver="${NEXT#v}"
            IFS='.' read -r MAJ MIN PAT <<<"$ver"
            NEXT="v${MAJ}.${MIN}.$((PAT+1))"
          done

          echo "Tagging $NEXT"
          git tag -a "$NEXT" -m "Release $NEXT"
          git push "https://x-access-token:${{ secrets.AREA_PUSH_TOKEN }}@github.com/${{ github.repository }}" "$NEXT"
          echo "TAG=$NEXT" >> $GITHUB_OUTPUT
        env:
          GIT_ASKPASS: /bin/echo
          GITHUB_TOKEN: ${{ secrets.AREA_PUSH_TOKEN }}

      - name: Checkout the created tag
        uses: actions/checkout@v6
        with:
          ref: ${{ steps.create_tag.outputs.TAG }}
          fetch-depth: 0
          submodules: true

      - name: Set BUILD_DATE (UTC ISO8601)
        id: build_date
        run: echo "BUILD_DATE=$(date -u +%Y-%m-%dT%H:%M:%SZ)" >> $GITHUB_OUTPUT

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.AREA_PUSH_TOKEN }}

      - name: Log in to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.AREA_DOCKERHUB_USERNAME }}
          password: ${{ secrets.AREA_DOCKERHUB_TOKEN }}

      - name: Compute image tags
        id: compute_tags
        env:
          GHCR_IMAGE: ghcr.io/${{ github.repository_owner }}/area-worker
          DH_IMAGE: ${{ secrets.AREA_DOCKERHUB_USERNAME }}/area-worker
          REF_NAME: ${{ steps.create_tag.outputs.TAG }}
        run: |
          {
            echo "TAGS<<EOF"
            if [[ "${REF_NAME}" == *"-"* ]]; then
              base_tag="$(echo "$REF_NAME" | cut -d'-' -f 2)-latest"
              echo "${GHCR_IMAGE}:${base_tag}"
              echo "${DH_IMAGE}:${base_tag}"
            else
              echo "${GHCR_IMAGE}:latest"
              echo "${DH_IMAGE}:latest"
            fi
            echo "${GHCR_IMAGE}:${REF_NAME}"
            echo "${DH_IMAGE}:${REF_NAME}"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          build-args: |
            BUILD_DATE=${{ steps.build_date.outputs.BUILD_DATE }}
            VCS_REF=${{ github.sha }}
          tags: ${{ steps.compute_tags.outputs.TAGS }}
          platforms: linux/amd64,linux/arm64
